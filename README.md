# Smart Copy - Google Chrome extension

1) `git clone git@github.com:hamelumster/smart-copy-from-ai-chats.git`
2) enable developer mode in Chrome: in browser line write `chrome://extensions` -> `download the unpacked extension` -> choose folder `smartcopy extension`

- For example, in a chat with AI, highlight the text (including code blocks);
- then right-click and select `SmartCopy — Copy as Markdown`; 
- then paste the text into an editor that supports markdown, for example `Obsidian`